var result = 0;

switch(operation) {
    case'+': result = a + b;
    case'-': result = a - value.b;
    case'/': result = a / b;
    case'*': result = a * b;
    case'%': result = a % b;
    case'*': result = Math.pow(a, b);
}
